version: '3'

services:
    sciencebeam-utils:
        build:
            context: .
            args:
                base_image: python:3.6.8-stretch
                version: ${VERSION}
        image: elifesciences/sciencebeam-utils:${IMAGE_TAG}

networks: {}
